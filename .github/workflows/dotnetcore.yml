name: .NET Core

on: 
    push:
        tags:
        - '*.*.*'
jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v5
    - name: Get tag
      id: tag
      uses: dawidd6/action-get-tag@v1
    - name: Setup .NET 10
      uses: actions/setup-dotnet@v5
      with:
        dotnet-version: '10.0.x'
        dotnet-quality: 'preview'
    - uses: nuget/setup-nuget@v1
      with:
        nuget-api-key: ${{ secrets.NUGET_APIKEY }}
        nuget-version: '5.x'
    - name: Build Library
      run: dotnet build -p:PackageVersion=${{steps.tag.outputs.tag}} --configuration Release ./AsyncPlinq/AsyncPlinq.csproj
    - name: Publish Library
      run: dotnet nuget push .\AsyncPlinq\bin\Release\*.nupkg -k ${{ secrets.NUGET_APIKEY }} -s https://api.nuget.org/v3/index.json --skip-duplicate
